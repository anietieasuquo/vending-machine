import { jest } from '@jest/globals';

beforeEach(async () => {
  // jest.useFakeTimers();
  // jest.setTimeout(100000000);
});

afterEach(() => {
  // jest.clearAllTimers();
  // jest.clearAllMocks();
});
